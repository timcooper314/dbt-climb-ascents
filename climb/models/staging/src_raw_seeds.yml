version: 2

sources:
  - name: raw
    description: Data generated or sourced from Kaggle.
    database: AWSDataCatalog
    schema: dev_dbt_sector
    tables:
      - name: raw_routes
      - name: raw_climbers
      - name: raw_grades_ref
      - name: raw_ascents
