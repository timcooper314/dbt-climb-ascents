version: 2

seeds:
  - name: raw_grades_ref
    description: Raw reference data from CSV, containing mappings to French Sport climbing grades.
    columns:
      - name: grade_id
        tests:
          - not_null
      - name: grade_fra
    source:
      path: raw_grades_ref.csv
